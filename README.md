# inf-infra
Internal orchestration and infrastructure for projects with multiple repositories. Deploys the internal Traefik router, provisions isolated private networks, and serves as the main Git submodule orchestrator for the Infinyti framework architecture.
